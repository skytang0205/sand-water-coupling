#include "GeometryGenerator.h"

namespace PhysX {

GeometryGenerator::Data GeometryGenerator::createBox(float width, float height, float depth)
{
	Data ret;
	Vector3f length(width, height, depth);
	for (int axis = 0; axis < 3; axis++) {
		for (int dir = 0; dir < 2; dir++) {
			
		}
	}
}

}
